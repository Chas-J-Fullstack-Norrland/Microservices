eureka:
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/

spring:
  datasource:
    url: jdbc:postgresql://postgres-booking:5432/booking_service
    driver-class-name: org.postgresql.Driver
    username: admin
    password: password
  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: secret-key-here